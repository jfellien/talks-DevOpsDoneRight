@model IndexViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>We are running on an @Model.Environment environment, Version @Model.EnvironmentVersion, Location @Model.Location</p>
</div>